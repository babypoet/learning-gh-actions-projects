name: 04 - Using Actions

on: 
   workflow_dispatch:


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: chekout code
              uses: actions/checkout@v4
            - name: Printing Folders
              run: ls

