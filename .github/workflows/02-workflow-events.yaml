name: 02 - workflow-events.yaml


on: 
    pull_request:
    workflow_dispatch:


jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "Triggered by a(n) ${{ github.event_name}} event."